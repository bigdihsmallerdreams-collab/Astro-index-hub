--// Services
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local placeId = game.PlaceId

--// GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ServerToolsUI"
ScreenGui.Parent = player:WaitForChild("PlayerGui")

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0,260,0,230)
Frame.Position = UDim2.new(0.5,-130,0.5,-115)
Frame.BackgroundColor3 = Color3.fromRGB(25,25,25)
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true
Frame.Parent = ScreenGui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0,12)
Corner.Parent = Frame

-- Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1,0,0,40)
Title.BackgroundTransparency = 1
Title.Text = "Server Tools"
Title.TextColor3 = Color3.fromRGB(255,255,255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 20
Title.Parent = Frame

-- Server Hop Button
local HopButton = Instance.new("TextButton")
HopButton.Size = UDim2.new(0,180,0,35)
HopButton.Position = UDim2.new(0.5,-90,0.30,0)
HopButton.Text = "Server Hop"
HopButton.Font = Enum.Font.GothamBold
HopButton.TextSize = 18
HopButton.TextColor3 = Color3.new(1,1,1)
HopButton.BackgroundColor3 = Color3.fromRGB(0,170,255)
HopButton.Parent = Frame

Instance.new("UICorner", HopButton).CornerRadius = UDim.new(0,10)

-- FPS Optimizer Button
local OptButton = Instance.new("TextButton")
OptButton.Size = UDim2.new(0,180,0,35)
OptButton.Position = UDim2.new(0.5,-90,0.52,0)
OptButton.Text = "FPS Optimizer"
OptButton.Font = Enum.Font.GothamBold
OptButton.TextSize = 18
OptButton.TextColor3 = Color3.new(1,1,1)
OptButton.BackgroundColor3 = Color3.fromRGB(0,200,120)
OptButton.Parent = Frame

Instance.new("UICorner", OptButton).CornerRadius = UDim.new(0,10)

-- Super Optimizer Button
local SuperButton = Instance.new("TextButton")
SuperButton.Size = UDim2.new(0,180,0,35)
SuperButton.Position = UDim2.new(0.5,-90,0.74,0)
SuperButton.Text = "Super Optimizer"
SuperButton.Font = Enum.Font.GothamBold
SuperButton.TextSize = 18
SuperButton.TextColor3 = Color3.new(1,1,1)
SuperButton.BackgroundColor3 = Color3.fromRGB(255,120,0)
SuperButton.Parent = Frame

Instance.new("UICorner", SuperButton).CornerRadius = UDim.new(0,10)

-- FPS Counter
local FPSLabel = Instance.new("TextLabel")
FPSLabel.Size = UDim2.new(1,0,0,20)
FPSLabel.Position = UDim2.new(0,0,1,-20)
FPSLabel.BackgroundTransparency = 1
FPSLabel.TextColor3 = Color3.fromRGB(255,255,255)
FPSLabel.Font = Enum.Font.Gotham
FPSLabel.TextSize = 14
FPSLabel.Text = "FPS: ..."
FPSLabel.Parent = Frame

-- FPS Counter Logic
local frames = 0
local lastTime = tick()

RunService.RenderStepped:Connect(function()
    frames += 1
    if tick() - lastTime >= 1 then
        FPSLabel.Text = "FPS: "..frames
        frames = 0
        lastTime = tick()
    end
end)

--// Normal Optimizer
local function optimizeGame()

    Lighting.GlobalShadows = false
    Lighting.FogEnd = 100000

    for _,v in pairs(Lighting:GetChildren()) do
        if v:IsA("BloomEffect")
        or v:IsA("SunRaysEffect")
        or v:IsA("BlurEffect")
        or v:IsA("DepthOfFieldEffect")
        or v:IsA("ColorCorrectionEffect") then
            v.Enabled = false
        end
    end

    for _,v in pairs(workspace:GetDescendants()) do
        if v:IsA("ParticleEmitter")
        or v:IsA("Trail")
        or v:IsA("Smoke")
        or v:IsA("Fire")
        or v:IsA("Sparkles") then
            v.Enabled = false
        end
    end

    print("FPS Optimizer Enabled")
end

--// Super Optimizer
local function superOptimize()

    Lighting.GlobalShadows = false
    Lighting.FogEnd = 9e9
    Lighting.Brightness = 1

    for _,v in pairs(Lighting:GetChildren()) do
        if v:IsA("PostEffect") then
            v.Enabled = false
        end
    end

    for _,v in pairs(workspace:GetDescendants()) do

        if v:IsA("BasePart") then
            v.Material = Enum.Material.Plastic
            v.Reflectance = 0
        end

        if v:IsA("Decal") or v:IsA("Texture") then
            v.Transparency = 1
        end

        if v:IsA("ParticleEmitter")
        or v:IsA("Trail")
        or v:IsA("Smoke")
        or v:IsA("Fire")
        or v:IsA("Sparkles") then
            v.Enabled = false
        end
    end

    print("Super Optimizer Enabled")
end

--// Server Hop
local function getServers()
    local servers = {}

    local req = game:HttpGet(
        "https://games.roblox.com/v1/games/"..placeId.."/servers/Public?sortOrder=Asc&limit=100"
    )

    local data = HttpService:JSONDecode(req)

    for _,server in pairs(data.data) do
        if server.playing < server.maxPlayers then
            table.insert(servers, server.id)
        end
    end

    return servers
end

local function serverHop()

    while true do

        local servers = getServers()

        if #servers > 0 then

            local randomServer = servers[math.random(1,#servers)]

            local success = pcall(function()
                TeleportService:TeleportToPlaceInstance(placeId, randomServer, player)
            end)

            if success then
                break
            end
        end

        task.wait(2)
    end
end

--// Button Connections
HopButton.MouseButton1Click:Connect(serverHop)
OptButton.MouseButton1Click:Connect(optimizeGame)
SuperButton.MouseButton1Click:Connect(superOptimize)
